// DESC: u16 255 < 256 — proves both bytes compared
// EXPECT: mem8[0x6000] == 42
reg u8 PORTB @ 0x6000;

fn main() -> void {
  u16 a = 255;
  u16 b = 256;
  if (a < b) {
    PORTB = 42;
  } else {
    PORTB = 99;
  }
}
