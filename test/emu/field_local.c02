// DESC: local struct field load: Point{ .x=10, .y=42 }; read .y -> 42
// EXPECT: mem8[0x6000] == 42
reg u8 PORTB @ 0x6000;

struct Point {
  u8 x;
  u8 y;
}

fn main() -> void {
  Point p;
  p = Point{ .x = 10, .y = 42 };
  PORTB = p.y;
}
