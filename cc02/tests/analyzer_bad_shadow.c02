// Shadowing an outer scope's variable is rejected - see declare_local_variable()
// in analyzer.c for why (codegen keys variables by bare name, so two "x"s in
// nested scopes would alias the same storage).

fn main() -> void {
  u8 x = 1;

  if (x > 0) {
    u8 x = 2;
  }
}
