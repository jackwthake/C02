reg u8 PORTB @ 0x6000;
reg u8 DDRB @ 0x6002;

u8 x = 15; // Global variable at zero page address 0x40 
u8 z;
u8 y = 0xFF;

fn main() -> void {
  u8 w = 69; // so that the stack epilogue still generates
  DDRB = y;

  PORTB = x;
}