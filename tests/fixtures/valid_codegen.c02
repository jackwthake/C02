reg u8 PORTB @ 0x6000;
reg u8 DDRB @ 0x6002;

fn main() -> void {
  DDRB = 0xFF;
  u8 delay = 0;
  u8 x = 0;

  while (1) {
    delay = 0;
    while (delay != 255) {
      delay = delay + 1;
    }

    x = x + 1;
    PORTB = x;
  }
  PORTB = 0xAA;
}