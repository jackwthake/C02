reg u16  PORTB @ 0x6000;
reg u8 DDRB @ 0x6002;

fn main() -> void {
  DDRB = 0xFF;
  u8 delay = 0;
  u16 x = 2;

  x = __mul16(x, 3); // this call happens
  x = __div16(x, 2); // this call happens
  PORTB = x; // this write happens
}