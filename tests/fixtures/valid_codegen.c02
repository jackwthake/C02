reg u8 PORTB @ 0x6000;
reg u8 DDRB @ 0x6002;

fn main() -> void {
  DDRB = 0xFF;

  // Use an explicit local variable instead of reading directly from DDRB
  u8 state = 0xFF - 0x55; 
  u8 x = 0x55;
  
  PORTB = state + x; // Evaluates flawlessly to 0xFF in compiler registers!
}