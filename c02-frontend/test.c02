fn sum(u8 x, u8 y) -> u8 {
  return x + y;
}

u8 x =10;
u8 x;

reg u8 PORTA @ 0x6000;

fn irq() interrupt -> void {

}

decl fn nmi() interrupt -> void;

struct Point {
  u8 x;
  u8 y;
}

fn main () -> void {
  Point p = Point { .x = 1, .y = 2 + 3 };
  u8 a = p.x;
  u8 b = origin.pos.x;
  u8 c = get().field;
}